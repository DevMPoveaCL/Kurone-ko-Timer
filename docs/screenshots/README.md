# Screenshots

This directory contains public README screenshots for KURONE-KO.

Expected files:

- `dashboard.png` — the main dashboard window.
- `timer-widget.png` — the floating focus timer widget.

Capture these from the development app without running a production build. Do not commit Playwright reports, traces, videos, or `test-results/` artifacts.
